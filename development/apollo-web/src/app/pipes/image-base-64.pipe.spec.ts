import { ImageBase64Pipe } from './image-base-64.pipe';

describe('ImageBase64Pipe', () => {
  it('create an instance', () => {
    const pipe = new ImageBase64Pipe();
    expect(pipe).toBeTruthy();
  });
});
