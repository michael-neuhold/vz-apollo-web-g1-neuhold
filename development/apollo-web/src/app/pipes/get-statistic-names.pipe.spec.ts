import { GetStatisticNamesPipe } from './get-statistic-names.pipe';

describe('GetStatisticNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetStatisticNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
