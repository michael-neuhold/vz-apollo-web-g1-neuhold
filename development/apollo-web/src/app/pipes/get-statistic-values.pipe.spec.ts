import { GetStatisticValuesPipe } from './get-statistic-values.pipe';

describe('GetStatisticValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetStatisticValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
