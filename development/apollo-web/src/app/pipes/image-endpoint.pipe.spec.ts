import { ImageEndpointPipe } from './image-endpoint.pipe';

describe('ImageEndpointPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageEndpointPipe();
    expect(pipe).toBeTruthy();
  });
});
